@model List<CheckOut.Models.Checklist>

<h2>All Checklists</h2>
<ul>
@foreach (var checklist in Model)
{
    <li>
        <a asp-controller="Checklists" asp-action="Details" asp-route-id="@checklist.ChecklistId">
            @checklist.Title
        </a>
    </li>
}
</ul>